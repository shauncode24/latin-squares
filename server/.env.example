MONGODB_URI=mongodb://127.0.0.1:27017/dmat
JWT_SECRET=replace-with-a-long-random-string
CLIENT_ORIGIN=http://localhost:5173
PORT=4000
NODE_ENV=development

# Required for the AI coaching endpoints (/api/coach/*). If unset, those
# two features fail gracefully with a 500 — nothing else in the app depends
# on this key.
GROQ_API_KEY=